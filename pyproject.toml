[project]
name = "legal-ai-bot"
version = "1.0.0"
description = "WhatsApp-native Legal AI Bot for Indian Litigating Lawyers"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "moto>=5.0",
]
